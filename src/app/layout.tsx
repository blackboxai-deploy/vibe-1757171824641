import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Deriv Trading Dashboard | Blackbox AI',
  description: 'Advanced trading analytics and strategy management dashboard for Deriv markets',
  keywords: ['trading', 'deriv', 'analytics', 'blackbox', 'ai', 'dashboard'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}